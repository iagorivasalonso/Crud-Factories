import 'package:fluent_ui/fluent_ui.dart';


class newImport extends StatelessWidget {
  const newImport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ScaffoldPage(
      content: Center(
        child: Text("se meteran datos de archivo"),
      ),
    );
  }
}