import 'package:fluent_ui/fluent_ui.dart';


class viewEmail extends StatelessWidget {
  const viewEmail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ScaffoldPage(
      content: Center(
        child: Text("cuentas correo"),
      ),
    );
  }
}