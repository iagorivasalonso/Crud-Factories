import 'package:fluent_ui/fluent_ui.dart';


class newFactory extends StatelessWidget {
  const newFactory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ScaffoldPage(
      content: Center(
        child: Text("aqui ira la ficha de empres"),
      ),
    );
  }
}